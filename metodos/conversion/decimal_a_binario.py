"""Plantilla: conversión decimal -> binario.

Archivo de ejemplo / esqueleto. Renombrar o duplicar este patrón para cada
conversión real (ej. `decimal_a_octal.py`, `binario_a_decimal.py`, ...).
Un archivo por conversión/algoritmo.
"""

from __future__ import annotations


def decimal_a_binario(numero: int) -> str:
    """Convierte un entero decimal a su representación binaria.

    Args:
        numero: Entero en base 10 a convertir. Debe ser >= 0.

    Returns:
        Cadena con la representación binaria de `numero`.

    Raises:
        ValueError: Si `numero` es negativo.

    TODO(equipo): implementar el algoritmo. Validar con `utils.validaciones`
    si aplica (ej. verificar que numero sea entero no negativo).
    """
    raise NotImplementedError("Pendiente de implementar por el equipo.")
