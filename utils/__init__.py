"""Paquete `utils`: utilidades compartidas entre `interfaz` y `metodos`.

Módulos:
    validaciones -- validación de entradas (números, intervalos, listas)
    errores       -- excepciones propias del dominio del proyecto
    formato       -- formateo de resultados (tablas de iteraciones, etc.)
"""
